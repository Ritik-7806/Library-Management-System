apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lms-chart.fullname" . }}-test-connection"
  labels:
    {{- include "lms-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lms-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
